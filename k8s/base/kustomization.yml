apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: diary-config
    files:
      - application.yml
    options:
      labels:
        app: diary-service